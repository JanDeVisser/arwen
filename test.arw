func write(fd: u32, bytes: *u8, num: u64) i64 -> "arwen$write"

func print(s: string) {
    write(1, ptr(s), len(s));
}

func println(s: string) {
    print(s);
    print("\n");
}

func main() {
    println("Hello, World!");
}
